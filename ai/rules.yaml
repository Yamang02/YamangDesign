# AI Agent Policy - Single Source of Truth
# 이 파일에서 .claudeignore, .cursorignore 등이 생성됨
# 코딩 규칙은 .cursor/skills/yamang-design-stack/SKILL.md로 이관됨

ignore:
  # Build outputs
  - dist/**
  - build/**
  - coverage/**
  - node_modules/**

  # Epic docs (작업 중일 때만 참조)
  - docs/epic/**
