# P03: Ch.3 Application Section

## 목표

- Ch.3에서 Starry Night 팔레트 × DS 토큰 4블록(Color Tokens, Buttons, Typography, Art Card)이 렌더된다.
- Art Card에 styleTag `'Post-Impressionism'`이 표시된다.
- 페이지 전체가 완성된 상태로 `npm run lint` + `npx tsc --noEmit`을 통과한다.

## 구현 상세

### 접근

기존 `ArtApplicationSection` 공유 컴포넌트에 `variant="minimal"` 적용. P01에서 정의한 `STARRY_NIGHT_PALETTE` 상수를 재사용.

### 데이터 상수

```ts
// VanGoghStarryNight.tsx 내 상수
const BUTTONS = [
  { label: 'Primary',   hex: '#1B3A8C', textHex: '#F0E8C8' },
  { label: 'Secondary', hex: '#3D2B6B', textHex: '#F0E8C8' },
  { label: 'Accent',    hex: '#F5C842', textHex: '#0D1B3E' },
  { label: 'Ghost',     hex: 'transparent', textHex: '#0D1B3E' },
];

const TYPOGRAPHY = [
  { level: 'Display', text: 'The Starry Night',                     hex: '#0D1B3E' },
  { level: 'Body',    text: 'I dream of painting and then I paint my dream.', hex: '#1A2A1A' },
  { level: 'Caption', text: 'Vincent van Gogh, 1889',               hex: '#1B3A8C' },
];

const ART_CARD = {
  imageUrl: '/art/starry-night/hero.jpg',
  title: 'The Starry Night',
  artist: 'Vincent van Gogh',
  year: 1889,
  movement: 'Post-Impressionism',
  styleTag: 'Post-Impressionism',
};
```

### 변경 범위

**수정**
- `src/app/pages/art/VanGoghStarryNight/VanGoghStarryNight.tsx` — Ch.3 `ArtApplicationSection` 추가 및 CHAPTERS 배열 완성

## 체크리스트

- [ ] Ch.3 섹션에서 8색 Color Tokens 칩이 표시된다
- [ ] Primary / Secondary / Accent / Ghost 버튼이 팔레트 색상으로 표시된다
- [ ] Display / Body / Caption 타이포그래피 3단계가 표시된다
- [ ] Art Card에 Starry Night 이미지와 `Post-Impressionism` 태그가 표시된다
- [ ] `npm run lint` 경고 없음
- [ ] `npx tsc --noEmit` 에러 없음
