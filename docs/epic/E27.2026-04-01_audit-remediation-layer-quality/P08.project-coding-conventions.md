# P08: 프로젝트 코딩 컨벤션 문서·도구 설정

## 목표

- E26 P06·E27 전반에서 도출된 **저장소 단위** 규칙을 `docs/process/CODING_CONVENTIONS.md`로 한곳에 모은다.
- **ESLint**로 레이어 금지(`@app` ← domain/shared)·**react-hooks/exhaustive-deps**를 error로 고정한다.
- **EditorConfig**로 들여쓰기·줄바꿈 기준을 공유한다.
- **AGENTS**·**ARCHITECTURE** 문서 목록에서 본 문서를 찾을 수 있다.

## 구현 상세

### 접근

1. 컨벤션 문서: 레이어, TS, lint 파이프라인, CC-05, 네이밍 샘플링, Git — E26/E27 Phase와 명시적 연결.
2. `eslint.config.js`: `react-hooks/exhaustive-deps` = `error`; `src/domain`·`src/shared`에 `no-restricted-imports` 패턴 `@app`.
3. `.editorconfig`, `.cursor/rules/yamang-coding-conventions.mdc` 보조.

### 변경 범위

- `docs/process/CODING_CONVENTIONS.md`, `eslint.config.js`, `.editorconfig`, `.cursor/rules/yamang-coding-conventions.mdc`
- `AGENTS.md`, `docs/design/ARCHITECTURE.md` (문서 목록)

## 완료 기록 (2026-04-01)

- 위 파일 반영. 기존 코드베이스는 `@app` 미사용(domain/shared)으로 제약 통과.

## 체크리스트

- [x] `docs/process/CODING_CONVENTIONS.md`가 E26/E27 보완사항을 프로젝트 관점으로 요약한다
- [x] ESLint가 domain/shared → `@app` import를 금지한다
- [x] `react-hooks/exhaustive-deps`가 error이다
- [x] `npm run lint` 통과
