# P02: Ch.1 — The Painting

## 목표

원화 이미지 + MuseumLabel + PaletteSwatchBar가 표시되는 Ch.1 완성.

## 구현 상세

**수정 파일:**
- `src/app/pages/art/MondriComposition/MondriComposition.tsx` — Ch.1 섹션 추가
- `src/app/pages/art/MondriComposition/MondriComposition.module.css` — page, anchorTarget, chapterTransition 스타일

**공유 컴포넌트 활용:**
- `ArtHeroStage` — imageUrl, imageAlt, label, palette props 전달
  - variant 필요 여부 판단: minimal 스타일이 기존 default/brutal과 충분히 다르면 추가, 아니면 default 재사용
- `MuseumLabel` — label prop으로 렌더 (ArtHeroStage 내부 처리)
- `PaletteSwatchBar` — palette prop으로 렌더

**MuseumLabel 텍스트:**
```
Composition with Large Blue Plane,
Red, Black, Yellow, and Gray
──
Piet Mondrian
Oil on Canvas, 1921
```

**이미지 URL:** `https://mdl.artvee.com/sftb/913118ab.jpg`

## 체크리스트

- [x] `MONDRI_LABEL` (LabelInfo) 상수 정의
- [x] `ArtHeroStage`에 imageUrl / imageAlt / label / palette 전달
- [x] `id="chapter-1"` 앵커 설정
- [x] `ArtHeroStage` / `PaletteSwatchBar`에 `variant="minimal"` 추가
- [x] `npx tsc --noEmit` 에러 없음
