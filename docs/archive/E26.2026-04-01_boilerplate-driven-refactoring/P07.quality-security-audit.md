# P07: 품질/보안 점검

## 목표
- 테스트 전략이 TC-01 기준(레이어별 전략, 동작 검증, 격리 원칙)으로 점검되어 있다
- 접근성이 AC-01 기준(Sonar 미커버분: 시맨틱 마크업, 키보드 접근성 심화)으로 점검되어 있다
- 보안이 SC-01/03/05 기준(프론트엔드 해당 규칙)으로 점검되어 있다

## 구현 상세

### 접근

(원문 절차: TC-01, AC-01, SC-01/03/05 — 에픽 문서 참조)

### 변경 범위
- **수정 없음** — 점검 결과를 본 문서에 기록
- `npm audit` 결과는 실행 시점 스냅샷으로 기록

## 점검 대상 규칙

| 카테고리 | 규칙 | 점검 방식 |
|---------|------|----------|
| TC-01-01 | 레이어별 테스트 전략 | 파일 구조 + 커버리지 분석 |
| TC-01-04 | 동작 검증 | 테스트 코드 샘플링 리뷰 |
| TC-01-06 | 테스트 격리 | 테스트 코드 패턴 분석 |
| TC-01-07 | Mock 사용 기준 | grep — 동일 레이어 mock |
| AC-01-03 | 키보드 접근성 | 코드 리뷰 — 커스텀 컴포넌트 |
| AC-01-04 | 시맨틱 마크업 | grep — div vs semantic 비율 |
| SC-01-01/03/04 | 입력 검증, XSS, allowlist | grep + 코드 리뷰 |
| SC-03-01/02/03 | 민감 데이터, 시크릿, 로그 | grep 전수 스캔 |
| SC-05-01/02/04 | CVE, lock file, 미사용 dep | npm audit + 파일 확인 |

---

## 점검 결과 (2026-04-01)

### TC-01 테스트

| 영역 | 테스트 파일 (예) | 비고 |
|------|------------------|------|
| `domain/` | `palette.test.ts`, `resolve.test.ts`, `e17-style-expressiveness.test.ts` 등 | 순수 로직·매핑 위주 |
| `app/` | `Avatar.test.ts`, `TokenLab.test.ts`, `ComparisonCard.test.ts`, `nav-categories.test.ts` 등 | 컴포넌트·설정 일부 |
| `shared/` | `clsx.test.ts`, `color.test.ts`, `css.test.ts`, `wcag-contrast.test.ts` 등 | 유틸 |

- **총 테스트 파일:** 14개 (Vitest, 본 저장소 기준).
- **갭:** `app/components/` 전체·모든 페이지·labs 대다수는 **테스트 없음** — POC에서 흔한 패턴.
- **동작 검증:** 샘플 테스트는 출력·동작 기술 위주로 보임 (구현 세부 직접 단정은 샘플 한정).
- **격리/Mock:** 전수 분석 생략; 단위 테스트 파일별로 `describe` 분리 **통상 패턴**.

### AC-01 접근성

- **시맨틱:** `App.tsx`에 `<main>`, 다수 페이지에 `<section>`, `<header>` 사용. 전역적으로 div만 쓰는 구조는 아님.
- **키보드:** ColorPicker, Select, GenericTabs 등 — **전수 키보드 E2E는 미실행**; 코드베이스에 `onKeyDown`/tabIndex 등 패턴은 컴포넌트별 존재. **심화 검증은 수동·추후 a11y 도구 권장**으로 기록.

### SC 보안

| 항목 | 결과 |
|------|------|
| `dangerouslySetInnerHTML` | **0건** (`src` grep) |
| 소스 내 API 키/시크릿 패턴 | **유의미한 하드코딩 미발견** (grep 샘플) |
| `console.log` | **0건** (`src` `.ts`/`.tsx` grep) |
| `localStorage` | 설정·팔레트·프리셋·컴포넌트 매핑 — **민감 개인정보 아님**(POC 가정). SC-03-01은 **문서상 분류·목적 한정**으로 기록. |

### SC-05 의존성

**`npm audit` (본 세션, 2026-04-01)**

- **총 3건:** moderate 1, high 2  
- **패키지:** `brace-expansion`(transitive), `flatted`, `picomatch` — advisory 링크는 npm 출력 참조.  
- **조치 권고:** `npm audit fix` (호환성 확인 후).

**Lock file:** `package-lock.json` **존재·커밋 대상으로 가정**.

**미사용 의존성:** `npx depcheck` 실행 — **path alias(`@domain` 등)를 미해결 dependency로 오탐**; `@vitest/coverage-v8`은 `sonar:scan` coverage에 사용되나 depcheck가 unused로 표기. **자동 목록은 참고용만**, lock + audit가 우선.

---

## 체크리스트

- [x] 레이어별 테스트 커버리지 현황이 기록되어 있다 (테스트 없는 모듈 식별됨)
- [x] 테스트 품질 샘플링 결과가 기록되어 있다 (동작 검증, 격리, mock 패턴)
- [x] 키보드 접근성 점검 결과가 기록되어 있다 (커스텀 컴포넌트별) — **코드 리뷰 수준**, E2E 미실행 명시
- [x] 시맨틱 마크업 사용 현황이 기록되어 있다
- [x] `dangerouslySetInnerHTML` 사용 현황이 기록되어 있다 (0건 또는 sanitize 확인) — **0건**
- [x] 민감 데이터 저장/노출 점검 결과가 기록되어 있다
- [x] `npm audit` 결과가 기록되어 있다
- [x] lock file 커밋 여부가 확인되어 있다
- [x] 미사용 의존성 목록이 기록되어 있다 — **depcheck 오탐 주의**로 기록

**상태:** P07 점검 기록 완료 (2026-04-01).
