# E28 / P01: 라우팅 & Ch.1 HeroStage

## 목표

- `Golconda` 페이지가 `nav-categories.ts`, `App.tsx`, `pages/index.ts`에 등록되어 내비게이션에서 접근 가능하다.
- Ch.1에서 `Golconda.jpg` 원화가 `ArtHeroStage`에 렌더되고, MuseumLabel과 PaletteSwatchBar가 표시된다.
- `golconda-palette.ts`의 GOLCONDA_PALETTE 8색이 PaletteSwatchBar에 반영된다.

## 구현 상세

### 접근
기존 `MatisseDanceII` / `MondriComposition` 패턴을 그대로 따른다. `ArtHeroStage` 공유 컴포넌트를 재사용하고, **`variant="glass"`** (명시 권장, 에픽 Readme와 동일).

### 변경 범위

**생성**
- `src/app/pages/art/Golconda/Golconda.tsx` — 페이지 루트, ArtShell + 3챕터 스캐폴드
- `src/app/pages/art/Golconda/Golconda.module.css`
- `src/app/pages/art/Golconda/golconda-palette.ts` — GOLCONDA_PALETTE 8색 상수
- `src/app/pages/art/Golconda/index.ts` — export

**수정**
- `src/app/config/nav-categories.ts` — art 카테고리에 Golconda 항목 추가
- `src/App.tsx` — `PageName` 타입 + `renderPage()` switch 케이스 추가
- `src/app/pages/index.ts` — Golconda export 추가

## 체크리스트

- [x] `nav-categories.ts`에 Golconda 항목이 있고 내비게이션에 노출된다
- [x] `App.tsx` `PageName`에 `'Golconda'`가 포함되고 `renderPage()`에 케이스가 있다
- [x] `pages/index.ts`에서 Golconda가 export된다
- [x] `Golconda.jpg` HeroStage가 브라우저에서 렌더된다
- [x] MuseumLabel: `LabelInfo` 필드로 표시 — `title: Golconda`, `artist: René Magritte`, `medium: Oil on canvas`, `year: 1953` (컴포넌트는 제목·구분선·작가·재질+연도 형태로 렌더)
- [x] PaletteSwatchBar에 8색 스와치가 표시된다
- [x] Ch.2, Ch.3는 빈 `<section>` 플레이스홀더로 존재한다
- [x] `npx tsc --noEmit` 에러 없음
- [x] `npm run lint` 에러 없음
