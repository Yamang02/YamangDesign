# P03: Ch.3 Application Section

## 목표

- Ch.3에서 Starry Night 팔레트 × DS 토큰 4블록(Color Tokens, Buttons, Typography, Art Card)이 렌더된다.
- Art Card에 styleTag `'Post-Impressionism'`이 표시된다.
- 페이지 전체가 완성된 상태로 `npm run lint` + `npx tsc --noEmit`을 통과한다.

## 구현 상세

### 접근

기존 `ArtApplicationSection` 공유 컴포넌트에 `variant="minimal"` 적용. P01에서 정의한 `STARRY_NIGHT_PALETTE` 상수를 재사용.

### 데이터 상수

`VanGoghStarryNight.tsx` 내 버튼·타이포·아트 카드 메타 — 팔레트는 `starry-night-palette.ts`와 연동.

### 변경 범위

**수정**
- `VanGoghStarryNight.tsx` — Ch.3 `ArtApplicationSection` 및 CHAPTERS

## 체크리스트

- [x] Ch.3 섹션에서 8색 Color Tokens 칩이 표시된다
- [x] Primary / Secondary / Accent / Ghost 버튼이 팔레트 색상으로 표시된다
- [x] Display / Body / Caption 타이포그래피 3단계가 표시된다
- [x] Art Card에 Starry Night 이미지와 `Post-Impressionism` 태그가 표시된다
- [x] `npm run lint` 경고 없음
- [x] `npx tsc --noEmit` 에러 없음
