# P04: 의존성·audit

## 목표

- `npm audit`에서 보고된 **moderate/high** 이슈가 해소되거나, 도구가 제안하는 업그레이드가 **호환 범위 내**에서 적용된다.
- 적용 후 **회귀:** `npm test`, `npm run build` 통과.

## 구현 상세

### 접근

1. E26 P07 스냅샷: `brace-expansion`, `flatted`, `picomatch` 등 **transitive** 포함.
2. `npm audit fix` 후 lockfile 갱신; 깨지면 범위 축소·개별 패키지 버전 조정.
3. **완전 해소 불가** 시: CVE·버전·Accepted 근거를 본 Phase `특이점` 또는 에픽 `특이점`에 한 줄 기록.

### 변경 범위 (예상)

- `package.json` / `package-lock.json`

## 완료 기록 (2026-04-01)

- **조치:** `npm audit fix` — lockfile 갱신(4 packages changed).
- **사후 `npm audit`:** `found 0 vulnerabilities`.
- **회귀:** `npm test` 14 files / 129 tests 통과, `npm run build` 성공.

## 체크리스트

- [x] `npm audit` 실행 결과가 **기록**됨(본 Phase 완료 시점)
- [x] moderate/high가 **0**이거나, 남은 항목에 **근거**가 문서화됨
- [x] `npm test` 및 `npm run build` 성공
